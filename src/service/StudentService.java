package service;

import model.Student;

public interface StudentService {
    void inputaddStudent(Student student);
    void intoaddStudent(Student student);

     int findMaxAge(Student student);
}
